import { DuplicateNameDirective } from './duplicate-name.directive';

describe('DuplicateNameDirective', () => {
  it('should create an instance', () => {
    const directive = new DuplicateNameDirective();
    expect(directive).toBeTruthy();
  });
});
